services:
    api:
      build: ./api
      ports:
        - "5000:5000"
      volumes:
        - './api/Database:/api/Database'
        - './api/Logs:/api/Logs'
    frontend:
      build: ./frontend
      ports:
        - "8080:80"
